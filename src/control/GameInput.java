package control;

import org.newdawn.slick.Input;

public class GameInput{

	public static void processInput(Input input, int delta) {
		if (input.isMouseButtonDown(Input.MOUSE_LEFT_BUTTON)) {
			//placeTile(Game.getCurrentTileType())
		}
	}
}
